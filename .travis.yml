language: python
python:
  - "2.6"
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
install:
  - pip install -r requirements.txt
  - pip install nose
script:
  - cd src
  - nosetests
  - cd ..
  - pip install .
  - which keepserver
  - keepserver --help
